---
ansible-navigator:
  execution-environment:
    image: aap.lab.example.com/ee-supported-rhel8:latest
    pull:
      policy: missing
  mode: stdout
  logging:
    file: /dev/null
  playbook-artifact:
    enable: false
